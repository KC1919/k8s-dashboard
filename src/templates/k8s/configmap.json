{
  "apiVersion": "v1",
  "kind": "ConfigMap",
  "metadata": { "name": "{{configMapName}}" },
  "data": {
    {{#each data}}
    "{{@key}}": "{{this}}"{{#unless @last}},{{/unless}}
    {{/each}}
  }
}
